## anchora

anchora is a workflow engine for agents.

on a high level : this workflow engine for agents is a aystem that orchestrates
multiple AI agents (or tools) through a defined execution graph, managing state,
dependencies and control flow between them.

it uses a simple state machine model to orcheastrate : execution follows explicit states
and transitions.


