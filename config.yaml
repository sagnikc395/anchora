server:
  address: ":8080"

workflow:
  max_retries: 2
  retry_delay_ms: 500

# Named Eino agents are optional. Set OPENAI_API_KEY (or each agent's
# api_key_env) before starting the server.
agents: {}
